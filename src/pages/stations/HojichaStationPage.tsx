import React from 'react';
import StationPage from './StationPage';

const HojichaStationPage: React.FC = () => {
  return <StationPage stationName="Drinks (Hojicha)" station="hojicha" categories={['Matcha']} />;
};

export default HojichaStationPage;
